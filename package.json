{
  "name": "vuepress-blog-template",
  "version": "0.0.1",
  "description": "A blog powered by VuePress",
  "authors": {
    "name": "MattyStacks",
    "email": "matthew.therault@gmail.com"
  },
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev blog",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build blog"
  },
  "license": "MIT",
  "devDependencies": {
    "@vuepress/theme-blog": "^2.3.3",
    "vuepress": "^1.9.9"
  }
}
